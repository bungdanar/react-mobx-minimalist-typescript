import { User } from '../../data-types/user'

type ServerTableState = {
  data: User[]
  loading: boolean
  errMessage: string
  pageCount: number
  rowCount: number
}

// const serverTable: ServerTableState = {

// }
